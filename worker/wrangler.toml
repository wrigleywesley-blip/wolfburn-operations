name = "wolfburn-meta-sync"
main = "src/index.ts"
compatibility_date = "2026-05-08"

# Optional: lock down which origin can call the worker.
# Leave unset for "*" while developing.
# [vars]
# ALLOWED_ORIGIN = "https://wrigleywesley-blip.github.io"

# Secrets are NOT defined here. Set them via:
#   wrangler secret put META_ACCESS_TOKEN
#   wrangler secret put META_AD_ACCOUNT_ID
#
# That keeps your Meta token out of source control.
